# Hannover Messe Scout

This repository contains two connected parts:

1. A scraping and scoring pipeline that builds CSV target lists from Hannover Messe exhibitor data.
2. A lightweight Next.js dashboard that displays those CSVs in a mobile-friendly format for use on the show floor.

The current use case is Zetamotion outreach at Hannover Messe: identifying manufacturers, partners, and existing CRM companies worth speaking to in person.

## What The App Does

The app is a static scouting dashboard built with Next.js, Tailwind CSS, and shadcn/ui. It discovers event folders under `events/`, reads each event's generated CSV files from that event's `output/` folder, and presents them as searchable target lists.

It is designed for someone walking the event and needing fast access to:

- company name
- booth number
- country
- score
- target type
- short overview
- company website
- Hannover profile
- CRM context, where available

Selecting a company opens a detail view with the main outreach context, including the official Hannover summary and website-derived summary if the enrichment pipeline found one.

## Event Folders

Each event lives in its own folder:

```text
events/
  hannover-messe/
    event.json
    data/
      hannover_exhibitors_raw.csv
      hannover_exhibitors_enriched.csv
      profile_cache.json
      website_profile_cache.json
    output/
      zetamotion_priority_meeting_targets.csv
      zetamotion_manufacturer_targets.csv
      zetamotion_partner_targets.csv
      zetamotion_relevant_companies.csv
      crm_hannovermesse_accounts_matches.csv
```

The home page lists discovered event folders. Clicking an event opens `/events/<event-slug>` with the same dashboard populated from that event's CSVs. Adding a new folder with the expected output CSV names automatically creates a page at build time.

## Main Datasets

For each event, the dashboard reads these CSVs from `events/<event-slug>/output/` when present:

- `zetamotion_priority_meeting_targets.csv`
  - Top-ranked companies for immediate outreach.
- `zetamotion_manufacturer_targets.csv`
  - Likely end-customer manufacturers where Zetamotion could sell inspection solutions directly.
- `zetamotion_partner_targets.csv`
  - Integrators, metrology vendors, AI inspection companies, machine builders, and related partners.
- `zetamotion_relevant_companies.csv`
  - The broader scored shortlist across manufacturers and partners.
- `crm_hannovermesse_accounts_matches.csv`
  - Existing CRM accounts that also appear in the Hannover exhibitor data.

These dataset mappings are defined in [src/lib/targets.ts](/C:/Users/mikeg/OneDrive/Documents/hannover-messe-companies/src/lib/targets.ts).

## How The Data Pipeline Works

The CSVs are generated by Python scripts in `scripts/`.

The main ranking flow:

- starts from official Hannover exhibitor data
- enriches exhibitor rows with Hannover profile metadata
- applies basic exclusion filters first
  - geography filters
  - unwanted categories such as consulting, ERP, generic cloud, and similar low-fit categories
- fetches company website metadata for plausible candidates only
  - homepage title
  - homepage meta description
  - homepage headings
  - likely about/company page metadata
- scores each company against Zetamotion’s target profile
  - flat-product manufacturers
  - metals
  - composites
  - textiles and fabrics
  - specialty glass
  - roofing products
  - inspection hardware and semi-automated systems
  - AI inspection companies and related partners
- outputs ranked CSV files to `output/`

There is also a CRM cross-reference flow that matches existing CRM companies against the exhibitor list and outputs separate CSVs for confirmed overlaps.

Relevant scripts:

- [scripts/build_target_lists.py](/C:/Users/mikeg/OneDrive/Documents/hannover-messe-companies/scripts/build_target_lists.py)
- [scripts/crossref_crm_hannover.py](/C:/Users/mikeg/OneDrive/Documents/hannover-messe-companies/scripts/crossref_crm_hannover.py)

## How The Dashboard Works

The dashboard is intentionally simple:

- server-side CSV loading happens in `src/lib/targets.ts`
- the app page loads all datasets and passes them into the UI
- the main interactive interface lives in `src/components/scout-dashboard.tsx`
- the UI uses shadcn components for buttons, cards, inputs, badges, separators, and sheets
- the theme uses a slate-style shadcn/Tailwind token setup

Key files:

- [src/app/page.tsx](/C:/Users/mikeg/OneDrive/Documents/hannover-messe-companies/src/app/page.tsx)
- [src/components/scout-dashboard.tsx](/C:/Users/mikeg/OneDrive/Documents/hannover-messe-companies/src/components/scout-dashboard.tsx)
- [src/app/globals.css](/C:/Users/mikeg/OneDrive/Documents/hannover-messe-companies/src/app/globals.css)
- [components.json](/C:/Users/mikeg/OneDrive/Documents/hannover-messe-companies/components.json)

## Local Development

Install dependencies:

```bash
npm install
```

Run the app locally:

```bash
npm run dev
```

Create a production build:

```bash
npm run build
```

Start the production build locally:

```bash
npm run start
```

## Refreshing The Data

If the target CSVs change, regenerate them with the Python scripts, then redeploy the app.

Typical flow:

```bash
python scripts/build_target_lists.py --event-slug hannover-messe --event-name "Hannover Messe"
python scripts/crossref_crm_hannover.py --event-slug hannover-messe --event-name "Hannover Messe"
```

For another Hannover-style export, provide a new slug and export URL:

```bash
python scripts/build_target_lists.py --event-slug example-event-2026 --event-name "Example Event 2026" --export-url "https://example.com/csvExport"
python scripts/crossref_crm_hannover.py --event-slug example-event-2026 --event-name "Example Event 2026"
```

The scripts create the event folder, `data/`, `output/`, and `event.json` automatically.

Optatec uses a different exhibitor source, so it has a dedicated ingestion script:

```bash
python scripts/build_optatec_target_lists.py --event-slug optatec --event-name "Optatec 2026"
```

That script downloads the official Optatec complete XLSX export, scrapes the paginated exhibitor cards and profiles, enriches company websites where available, scores the rows with Optatec-specific optics/metrology/manufacturing signals, and writes the same dashboard CSV filenames under `events/optatec/`.

The dashboard does not currently use a database. It is build-time/file-based and intended to stay lightweight.

## Deployment

The app is suitable for a basic Vercel deployment.

Current model:

- commit the repo with generated CSVs present under `events/<event-slug>/output/`
- deploy as a standard Next.js app
- when rankings are refreshed, redeploy

You do not need Supabase for the current version.

Supabase or another backend would only be useful if you want:

- live notes from the show floor
- per-target status updates
- shared editing
- authentication
- syncing without redeploying

## Notes

- The dashboard assumes each event uses the expected CSV filenames in `events/<event-slug>/output/`.
- If a CSV schema changes, `src/lib/targets.ts` may need to be updated to map fields correctly.
- The current CRM dataset shown in the app is the accounts match file, not the broader leads review set.
